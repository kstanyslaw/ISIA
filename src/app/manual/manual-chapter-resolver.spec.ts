import { ManualChapterResolver } from './manual-chapter-resolver';

describe('ManualChapterResolver', () => {
  it('should create an instance', () => {
    expect(new ManualChapterResolver()).toBeTruthy();
  });
});
